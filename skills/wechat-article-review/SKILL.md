---
name: wechat-article-reviewer
description: >
  审核微信公众号文章的综合技能。用户提供微信公众号文章的 URL 链接、粘贴全文或上传文件，技能优先通过 Browser Agent（浏览器自动化工具/MCP Browser 服务）访问文章链接获取内容，自动从四个维度进行审核：语言文字（错别字、语病、标点）、内容质量（观点凝练、逻辑性、可读性）、事实核查（数据、引用、人名等准确性）、格式编排（标题层级、图文排版、段落结构）。输出 Markdown 格式的审核报告，包含综合评分、问题清单和修改建议。
  当用户提到"审核公众号文章"、"帮我审稿"、"检查这篇文章"、"文章校对"、"公众号内容审核"、"review 这篇推文"，或者给出 mp.weixin.qq.com 链接并希望得到反馈，或者粘贴一段公众号文章内容要求审核时，都应触发此技能。即使用户只是说"帮我看看这篇文章有没有问题"，也应使用此技能。
---

# 微信公众号文章审核技能

## 工作流程

### 第一步：获取文章内容

微信公众号（`mp.weixin.qq.com`）有严格的反爬机制，`web_fetch` 通常会被 `robots.txt` 屏蔽。因此需要按优先级依次尝试以下方式获取内容：

**方式 A（推荐）：通过 Agent-Browser 工具自动化抓取**

使用 `agent-browser` 技能调用浏览器自动化 CLI，模拟真实用户浏览行为，可绕过 `robots.txt` 限制，是获取微信公众号文章内容的最佳方式。

调用方式分两步：

**步骤 1**：激活 `agent-browser` 技能。
- **Gemini CLI**: 使用 `activate_skill(name="agent-browser")` 工具。
- **Claude Code**: 使用 `Skill` 工具加载 `agent-browser` 文档。

**步骤 2**：使用 **Shell 运行工具**（Gemini CLI 为 `run_shell_command`，Claude Code 为 `bash`）依次执行以下 CLI 命令：

```bash
# 打开文章并等待页面加载完成
agent-browser open "{文章URL}" && agent-browser wait --load networkidle

# 提取标题、公众号名、发布时间、正文全文
agent-browser eval --stdin <<'EVALEOF'
JSON.stringify({
  title: document.querySelector('#activity-name')?.innerText?.trim() || '',
  author: document.querySelector('#js_name')?.innerText?.trim() || '',
  publishTime: document.querySelector('#publish_time')?.innerText?.trim() || '',
  content: document.querySelector('#js_content')?.innerText?.trim() || ''
})
EVALEOF
```

**提取要点：**
- 微信文章正文容器的选择器通常为 `#js_content`
- 需等待页面完全加载（networkidle），因为微信文章使用懒加载机制
- 同时提取标题（`#activity-name`）、作者（`#js_name`）、发布时间（`#publish_time`）等元信息
- 记录图片在正文中的相对位置，以辅助格式编排审核
- 避免执行任何登录操作或处理敏感信息，仅访问公开可见的文章页面

**方式 B：用户上传 Markdown 文件（备选）**
- 如果方式 A 不可用或抓取失败，请用户将文章内容整理为 **Markdown 文件**后上传
- Markdown 文件可完整保留标题层级、加粗、列表等格式，是格式编排审核最理想的备选方式
- 在报告中注明"格式编排审核基于所提供的 Markdown 文件，可能与公众号实际排版存在差异"

**方式 C：用户粘贴全文（兜底方案）**
- 如果方式 A 和 B 均不可用，请用户直接在对话中**粘贴文章全文**
- 提示用户尽量保留原始格式（标题层级、加粗、列表等），以便格式编排审核
- 在报告中注明"格式编排审核基于所提供内容的格式，可能与公众号实际排版存在差异"

**注意事项：**
- 无论通过哪种方式获取内容，都要先确认是否获取到完整正文，再进入审核流程
- 如果内容明显不完整（如只有标题没有正文），应提示用户补充

**环境兼容性说明：**

| 运行环境 | Agent-Browser 可用性 | 推荐方式 |
|----------|----------------------|----------|
| **Gemini CLI (本地)** | ✅ 可用 | 方式 A（Agent-Browser） |
| **Claude Code (本地)** | ✅ 可用 | 方式 A（Agent-Browser） |
| Claude Desktop + MCP | ✅ 可用 | 方式 A（Agent-Browser） |
| 网页端 AI (Claude/Gemini) | ❌ 不可用 | 方式 B/C（文件或粘贴） |

技能在执行时应按 A → B → C 顺序降级，不要让用户手动判断。

### 第二步：四维审核

获取到文章全文后，按以下四个维度依次审核。每个维度独立评分（满分 10 分），并记录发现的每个问题。

#### 维度一：语言文字（权重 30%）

检查以下方面：

- **错别字和用词错误**：同音字混淆（如"以至于"误写为"已至于"）、形近字混用、生造词语
- **语法和语病**：主语缺失、搭配不当、语序混乱、成分残缺或赘余、逻辑矛盾
- **标点符号**：标点误用（如中英文标点混用、引号层级错误、顿号与逗号混淆）、标点缺失或多余
- **用语规范**：专业术语使用是否准确、数字与单位写法是否规范、缩略语首次出现是否有解释

评分参考：
- 10 分：无任何语言文字问题
- 8-9 分：仅有 1-2 处轻微问题（如一个标点瑕疵）
- 6-7 分：有 3-5 处问题，但不影响理解
- 4-5 分：有多处问题，部分影响阅读
- 1-3 分：问题严重且密集，严重影响阅读体验

#### 维度二：内容质量（权重 30%）

检查以下方面：

- **观点凝练**：核心观点是否清晰突出、论述是否围绕主题展开、是否存在偏题或冗余内容
- **逻辑性**：论证过程是否严密、段落之间的过渡是否自然、因果关系是否成立、是否存在自相矛盾
- **可读性**：语言风格是否适合目标读者、段落长度是否合理、是否善用举例和类比帮助理解
- **标题与内容**：标题是否准确反映文章内容、是否存在"标题党"倾向

评分参考：
- 10 分：观点鲜明、逻辑严密、可读性极佳
- 8-9 分：整体优秀，个别段落可优化
- 6-7 分：基本达标，存在可改进空间
- 4-5 分：存在明显逻辑或表达问题
- 1-3 分：主题不清、逻辑混乱

#### 维度三：事实核查（权重 20%）

检查以下方面：

- **数据准确性**：文中引用的数字、统计数据、百分比是否有出处、是否合理
- **人名和机构**：人物姓名、职务、机构名称是否正确
- **引用来源**：引用的政策文件、研究报告、名人名言是否真实准确
- **时效性**：引用的信息是否过时，所述事件的时间线是否正确

**事实核查执行步骤（必须调用网页搜索工具）：**

1. 通读全文，列出所有可核查的事实性陈述（日期、数据、人名职务、引用观点、机构名称等）
2. 针对每条事实，使用 **网页搜索工具**（Gemini CLI 为 `google_web_search`，Claude Code 为 `google_search`）发起搜索，例如：
   - 核查会议日期：搜索 `NVIDIA GTC 2026 日期 地点`
   - 核查人物观点：搜索 `黄仁勋 GTC 2026 演讲 Agentic AI`
   - 核查产品信息：搜索 `卫宁健康 WiNBOT 发布时间`
3. 将每条事实的搜索结果记录为"✅ 准确 / ⚠️ 待核实 / ❌ 有误"，并在备注中说明依据
4. 对于搜索后仍无法确认的事实，标注"待核实"，不要凭模型记忆直接判定正误

评分参考：
- 10 分：所有可查证事实均准确无误
- 8-9 分：事实基本准确，极个别处表述不够精确
- 6-7 分：存在 1-2 处事实性偏差
- 4-5 分：存在多处事实错误或无法核实的关键信息
- 1-3 分：大量事实失实，严重影响可信度

#### 维度四：格式编排（权重 20%）

检查以下方面：

- **标题层级**：大小标题是否使用得当、层级是否清晰
- **段落结构**：段落划分是否合理、是否存在过长或过短的段落
- **列表与强调**：重点内容是否有合理的加粗、列表等视觉标记
- **整体节奏**：全文长度是否合适、是否存在明显的详略失当
- **排版一致性**：空行、缩进、字体使用是否统一

评分参考：
- 10 分：排版专业，阅读体验极佳
- 8-9 分：排版规范，仅有微小瑕疵
- 6-7 分：基本规范，有可改进之处
- 4-5 分：排版杂乱，影响阅读
- 1-3 分：缺乏基本的排版意识

### 第三步：生成审核报告并保存

按照下方的报告模板生成审核结果，然后使用 **文件写入工具**（Gemini CLI 为 `write_file`，Claude Code 为 `write`）将报告保存为 Markdown 文件。

**文件命名规则**：`review-{公众号名}-{YYYYMMDD}.md`，保存到**用户的当前工作目录**（即调用此技能时的 `pwd`）。例如：`review-卫宁健康-20260324.md`。

报告使用 Markdown 格式，结构清晰、问题定位精确、建议具有可操作性。

## 报告模板

严格按照以下结构输出报告（根据实际审核结果填充内容）：

```markdown
# 📋 微信公众号文章审核报告

## 基本信息

| 项目 | 内容 |
|------|------|
| 文章标题 | {标题} |
| 作者/公众号 | {作者} |
| 发布日期 | {日期} |
| 审核日期 | {当天日期} |
| 文章字数 | {约 X 字} |

---

## 综合评分

| 审核维度 | 得分 | 权重 | 加权得分 |
|----------|------|------|----------|
| 语言文字 | X/10 | 30% | X.X |
| 内容质量 | X/10 | 30% | X.X |
| 事实核查 | X/10 | 20% | X.X |
| 格式编排 | X/10 | 20% | X.X |
| **综合得分** | | | **X.X/10** |

**总体评价**：{一句话总结文章质量，如"文章整体质量优秀，语言流畅、论证有力，建议修正个别数据引用后即可发布。"}

---

## 一、语言文字审核

### 发现的问题

> 如无问题，写"未发现语言文字问题。"

**问题 1**：{问题描述}
- 📍 位置：{引用原文片段}
- ❌ 问题：{具体说明}
- ✅ 建议：{修改建议}

（更多问题按此格式列出）

---

## 二、内容质量审核

### 发现的问题

**问题 1**：{问题描述}
- 📍 位置：{定位}
- ❌ 问题：{具体说明}
- ✅ 建议：{修改建议}

（更多问题按此格式列出）

---

## 三、事实核查

### 核查结果

> 列出文中涉及的关键事实及核查结论

| 序号 | 原文陈述 | 核查结果 | 备注 |
|------|----------|----------|------|
| 1 | {原文中的事实性陈述} | ✅ 准确 / ⚠️ 待核实 / ❌ 有误 | {说明} |

### 需要修正的事实

> 如无需修正，写"所有可核查事实均准确。"

**问题 1**：{事实性错误描述}
- 📍 原文：{引用}
- ❌ 问题：{与事实不符之处}
- ✅ 建议：{正确信息及修改方案}

---

## 四、格式编排审核

### 发现的问题

**问题 1**：{问题描述}
- 📍 位置：{定位}
- ❌ 问题：{具体说明}
- ✅ 建议：{修改建议}

---

## 📝 修改优先级

将所有问题按紧急程度分为三级：

### 🔴 必须修改（发布前必须处理）
1. {问题摘要}

### 🟡 建议修改（能显著提升文章质量）
1. {问题摘要}

### 🟢 可选优化（锦上添花）
1. {问题摘要}

---

## 结论

**发布建议**：{三选一：建议直接发布 / 修改后发布 / 建议重大修改后重新审核}

{最后的总结性说明，肯定文章优点，概括需改进之处}
```

## 重要原则

1. **客观公正**：审核应基于事实和专业标准，避免主观偏好影响判断
2. **问题定位精确**：每个问题都要引用原文片段，方便作者定位和修改
3. **建议可操作**：不要只指出问题，还要给出具体的修改方案或替代表述
4. **肯定优点**：在总结中适当肯定文章的优点，平衡批评和鼓励
5. **事实核查谨慎**：对于无法确认的事实，标注"待核实"，不要想当然地判定错误
6. **必须实际搜索**：事实核查维度要求逐条调用 **网页搜索工具**（`google_web_search` / `google_search`），不能仅凭模型记忆判断；但不要过度搜索显而易见的常识。
